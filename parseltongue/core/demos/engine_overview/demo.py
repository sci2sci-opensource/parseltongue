"""
Demo: Engine Self-Inspection

The parseltongue engine inspects itself — loading its own formal specification
(core.pltg) and rendering an interactive notebook that shows the engine's
structure, consistency layers, error classification, and diff mechanism.

A pgmd notebook with inline computed values, taint propagation, margin pills,
derivation paths, and a full graph view of 2000+ nodes.
"""
